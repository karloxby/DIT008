In each of 1-4 represent the common form of each argument
using letters to stand for component sentences, and
fill in the blanks so that the argument in part (b) has the
same logical form as the argument in part (a).


2.1.2.		a. If all computer programs contain errors, then
		this program contains en error.
		This program contains an error.
		Therefore, it is not the case that all computer
		programs contain errors.

		b. If ________, then ________.
		2 is not odd.
		Therefore, it is not the case that all prime
		numbers are odd.


		a. 	If all computer programs contain errors, then
			this program contains an error.

			This program contains an error.

			Therefore, it is not the case that all computer
			programs contain errors.


			Let p =		All computer programs contain
					errors.

			Let q =		This program contains an error.

Solution:		p → q
			q
			∴ ~p



